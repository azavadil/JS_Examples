/**
 * Browser Object Model
 * --------------------
 * Location 
 * 
 * replace() method. replace the current page, doesn't go into history 
 * so you ability for user to use back button
 * 
 * reload() is reloaded in the most efficient way possible (may be from the cache). 
 * to force reload from server, pass true as an argument
 */ 
 
location.reload()
location.reload(true)

